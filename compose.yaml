services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: eventrelay
      POSTGRES_USER: eventrelay
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - eventrelay-postgres:/var/lib/postgresql/data

volumes:
  eventrelay-postgres: